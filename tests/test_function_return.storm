func retString() {
	return "hello\n";
}

x = retString();
assert(x, "hello\n", true); 

func retVal(value) {
	value = 3;
	return value;	
}

b = retVal(2);
assert(b, 3, true);

func retNum() {
	return 3;
}

n = retNum();
assert(n, 3, true);