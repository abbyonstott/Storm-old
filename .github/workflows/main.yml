name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        cmake .
        make -j$(nproc)
    - name: Test
        cd scripts
        ./test.sh